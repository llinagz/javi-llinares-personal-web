<section class="section">
  <div class="container">
    <div class="rounded-3xl border border-border bg-primary p-8 text-white md:p-12">
      <div class="grid gap-8 lg:grid-cols-[1fr_420px] lg:items-center">
        <div>
          <p class="text-sm font-semibold uppercase tracking-[0.2em] text-white/70">Notas futuras</p>
          <h2 class="mt-3 font-serif text-3xl font-semibold tracking-tight md:text-5xl">
            Escribo sobre programación, producto, lectura, cultura y la búsqueda de una vida con más sentido.
          </h2>
          <p class="mt-5 text-white/78">
            Sin ruido ni promesas grandilocuentes: solo ideas trabajadas, proyectos en marcha y notas con calma.
          </p>
        </div>

        <form class="rounded-2xl bg-white p-3 shadow-quiet" action="#" method="post">
          <!-- TODO: Connect newsletter provider when the publishing workflow is decided. -->
          <!-- TODO: Conectar proveedor de newsletter cuando se decida el flujo de publicación. -->
          <label class="sr-only" for="email">Email</label>
          <div class="grid gap-3 sm:grid-cols-[1fr_auto]">
            <input
              class="min-h-12 rounded-xl border border-border px-4 text-text placeholder:text-muted"
              id="email"
              name="email"
              type="email"
              placeholder="tu@email.com"
            />
            <button
              class="rounded-xl bg-primary px-5 py-3 text-sm font-semibold text-white transition hover:bg-primary-dark"
              type="submit"
            >
              Suscribirme
            </button>
          </div>
          <p class="mt-3 text-sm text-muted">
            También puedes escribirme directamente desde <a class="font-semibold text-primary hover:underline" href="mailto:hola@javillinares.com">hola@javillinares.com</a>.
          </p>
        </form>
      </div>
    </div>
  </div>
</section>
